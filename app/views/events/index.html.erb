<p id="notice"><%= notice %></p>

<h1>Inactive Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Date</th>
      <th>Time</th>
      <th>Location</th>
      <th>Points</th>
      <th>Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <% next if event.type_id != 5 %>
        <td><%= event.Title %></td>
        <td><%= event.Description %></td>
        <td><%= event.Date %></td>
        <td><%= event.Time %></td>
        <td><%= event.Location %></td>
        <td><%= event.Points %></td>
        <td><%= event.type.Value %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Archive', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
